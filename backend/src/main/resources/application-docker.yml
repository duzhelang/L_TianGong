spring:
  datasource:
    url: jdbc:mysql://mysql:3306/ecocarbon?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: ecocarbon
    password: ecocarbon123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:ecocarbon-mrv-jwt-secret-key-2024}
  expiration: ${JWT_EXPIRATION:7200000}

ai:
  api-key: ${AI_API_KEY}
  api-url: ${AI_API_URL}

logging:
  level:
    root: INFO
    com.ecocarbon: INFO
